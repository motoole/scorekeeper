import React from 'react';

import { 
  Typography
} from '@mui/material';


const SingleGame = (props) => {
  console.log(props);
  
  return(
    <Typography variant="h3">
      Manage Single Game Component
    </Typography>
  )
};

export default SingleGame;